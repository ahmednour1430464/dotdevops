version = "v0.6"
target "local" {
  address = "local"
}

step "deploy_app" {
  param dest_dir = "/opt/app"
  param backup_enabled = true
  
  type = file.sync
  src  = "./build"
  dest = dest_dir
}

node "production-deploy" {
  type    = deploy_app
  targets = [local]
}
